{
  "name": "webinar-frontend",
  "version": "1.0.0",
  "description": "Secure frontend for webinar platform",
  "scripts": {
    "start": "serve -s src -p 8080",
    "dev": "live-server --port=3000 --host=localhost",
    "build": "echo 'Static files - no build required'",
    "serve": "serve -s src -p 8080"
  },
  "devDependencies": {
    "live-server": "^1.2.0",
    "serve": "^14.2.1"
  }
}
