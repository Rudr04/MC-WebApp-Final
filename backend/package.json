{
  "name": "webinar-backend",
  "version": "1.0.0",
  "description": "Secure backend for webinar platform",
  "main": "src/index.js",
  "scripts": {
    "start": "NODE_ENV=production node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "build": "echo 'No build step required for backend'",
    "lint": "echo 'Add ESLint for production'",
    "prod:start": "NODE_ENV=production PM2_HOME=/tmp pm2 start src/index.js --name webinar-backend"
  },
  "dependencies": {
    "compression": "^1.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "express-validator": "^7.2.1",
    "firebase-admin": "^11.11.1",
    "helmet": "^7.2.0",
    "jsonwebtoken": "^9.0.2",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  }
}
